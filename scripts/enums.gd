extends Node

enum DragType { 
	PROJECT_TAB, 
	TOOL_PANEL
}

enum ToolType {
	CURSOR,
	TRANSFORM,
	PAINTBRUSH,
	PENCIL,
	ERASER,
	EYEDROPPER,
	PAINTBUCKET,
	RECTANGLE_SELECT,
	BRUSH_SELECT,
	LASSO_SELECT,
	MAGIC_WAND,
	PAN,
	ZOOM,
	TEXT,
	SHAPES
}

enum ViewMode {
	ALL_CHANNELS,
	ONLY_COLOR,
	RED,
	GREEN,
	BLUE,
	ALPHA
}
